# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'profile-test' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'PureLayout'
  # Pods for profile-test

  target 'profile-testTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'profile-testUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
